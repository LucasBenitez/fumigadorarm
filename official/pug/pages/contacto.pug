extends ../templates/template.pug
block append config
    - titlePage = "Contacto"
block append content
    section
        .container
            .row
                .col-12
                    +form(services, places)